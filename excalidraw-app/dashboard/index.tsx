import React from "react";

/**
 * Placeholder. The dashboard branch replaces this file with the real
 * dashboard and sign-in screens; the contract is this named export.
 */
export const DashboardApp: React.FC = () => {
  return (
    <div style={{ padding: "2rem", fontFamily: "Assistant, sans-serif" }}>
      <p>
        Dashboard not built yet. Open <a href="/local">/local</a> for the
        editor.
      </p>
    </div>
  );
};
